package one.digitalinnovation.digionebankcourse
import one.digitalinnovation.digionebankcourse.Autenticavel

class TesteLogin {
    fun testarLogin(autenticavel: Autenticavel) = println(autenticavel.login())
}